`timescale 1ns / 1ps

`include "defines.vh"

module execute_block
#(
    parameter D_SIZE = 32,
    parameter A_SIZE = 10
)
(
    // general
    input 		rst,   // active 0
    input		clk,
    
    input 
);



endmodule
